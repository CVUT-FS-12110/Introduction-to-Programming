Traceback (most recent call last):
  File "<string>", line 11, in <module>
  File "C:/msys64/mingw64/lib/python3.12/encodings/cp1250.py", line 19, in encode
    return codecs.charmap_encode(input,self.errors,encoding_table)[0]
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
UnicodeEncodeError: 'charmap' codec can't encode character '\u2192' in position 338: character maps to <undefined>
CELLS: 93
--- [0] MD ---
# Základy testování (Testing Fundamentals)

Tento notebook slouží jako úvod do **testování software** v Pythonu.
Nauèíte se:

- Co je testování a proè je dùležité
- Jak funguje klíèové slovo `assert`
- Jak psát a spouštìt testy pomocí knihovny **pytest**
- Jak pøemýšlet o **hranièních pøípadech** (edge cases)
- Jak vypadá vzor **Arrange-Act-Assert**

**Prerekvizity:** funkce, základy OOP, práce se soubory, knihovny a pip
--- [1] MD ---
---

## 1. Proè testujeme?

Pøedstavte si, že napíšete funkci, která poèítá prùmìr ze seznamu èísel.
Spustíte ji jednou, výsledek vypadá dobøe, odevzdáte. Ale co když:

- nìkdo pøedá **prázdný seznam**?
- vstup obsahuje **záporná èísla**?
- funkce je souèástí vìtšího projektu a za mìsíc ji **nìkdo zmìní**?

### Proè ne prostì `print()` a zkontroluju oèima?

Možná si øíkáte: *"Já si prostì pøidám `print(výsledek)` a podívám se, jestli to vypadá správnì."*
To funguje jednou. Ale:

| Print debugging | Automatické testy |
|---|---|
| Musíte **ruènì** kontrolovat výstup pokaždé | Spustíte jedním pøíkazem, výsledek OK/FAIL |
| Po opravì musíte **znovu** ruènì ovìøit vše | Testy ovìøí **všechno** automaticky za sekundy |
| Pøi 50 funkcích = 50× print + oèní kontrola | Pøi 50 funkcích = 1× `pytest` |
| Nemùžete sdílet s kolegou | Kolega spustí testy a ví, že kód funguje |

**Testování** znamená napsat kód, který automaticky ovìøuje, že se váš program chová správnì. Testy:

1. **Odhalí chyby døíve** - než je najde uživatel (nebo uèitel)
2. **Slouží jako dokumentace** - test ukazuje, jak se funkce má používat
3. **Dávají odvahu mìnit kód** - po každé zmìnì spustíte testy a víte, jestli jste nìco nerozbili

### Testování v dobì AI

> **V dobì AI je testování ještì dùležitìjší.** Pokud vám kód (nebo jeho èást) vygeneruje AI,
> testy jsou váš hlavní nástroj, jak ovìøit, že výsledek skuteènì dìlá to, co má.
>
> AI generátory kódu (jako GitHub Copilot nebo ChatGPT) jsou stále lepší, ale:
>
> - **Nevìdí pøesnì, co chcete** — mohou nepochopit hranièní pøípady
> - **Mohou produkovat kód s jemnými chybami** — kód vypadá správnì, ale nefunguje ve všech situacích
> - **Neumí ovìøit samy sebe** — to musíte udìlat vy, pomocí testù
>
> Schopnost napsat dobré testy je proto **dovednost, která ve vìku AI roste na cenì**, ne klesá.
--- [2] MD ---
---

## 2. Klíèové slovo `assert`

Python má vestavìné klíèové slovo `assert`, které funguje jako **kontrolní výraz**.
Zápis je jednoduchý:

```python
assert podmínka, "volitelná zpráva pøi selhání"
```

- Pokud je `podmínka` pravdivá (`True`), nic se nestane - program pokraèuje dál.
- Pokud je `podmínka` nepravdivá (`False`), Python vyhodí chybu `AssertionError`.

Pojïme si to vyzkoušet.
--- [3] CODE (8 ln) ---
# Nejdøíve si definujeme jednoduchou funkci, kterou budeme testovat
def add(a, b):
    """Return the sum of two numbers."""
    return a + b

# Assert projde - podmínka je True, nic se nestane
assert add(2, 3) == 5
print("Test prošel: add(2, 3) == 5")
--- [4] CODE (5 ln) ---
# Mùžeme øetìzit více assertù za sebou
assert add(0, 0) == 0
assert add(-1, 1) == 0
assert add(100, 200) == 300
print("Všechny testy prošly!")
--- [5] MD ---
### Co se stane, když assert selže?

Pokud je podmínka `False`, Python zastaví program a vyhodí `AssertionError`.
Volitelná zpráva nám pomùže pochopit, **co pøesnì selhalo**.
--- [6] CODE (2 ln) ---
# Tohle selže - úmyslnì špatný expected výsledek
assert add(2, 2) == 5, f"Oèekáváno 5, ale dostali jsme {add(2, 2)}"
--- [7] MD ---
Vidíte `AssertionError` se zprávou, kterou jsme zadali. Pøesnì tak funguje i pytest - jen to za nás hezky zformátuje.

### Další typy porovnání v assertech

Assert umí pracovat s libovolnou podmínkou, nejen `==`:
--- [8] CODE (20 ln) ---
# Porovnání rovnosti
assert add(1, 2) == 3

# Porovnání nerovnosti
assert add(1, 2) != 0

# Vìtší / menší
assert add(10, 20) > 25
assert add(1, 1) <= 2

# Kontrola typu
assert isinstance(add(1, 2), int)

# Kontrola, že hodnota je v seznamu
assert add(1, 2) in [1, 2, 3, 4, 5]

# Kontrola pravdivosti (truthy)
assert add(1, 0)  # nenulové èíslo je truthy

print("Všechny asserty prošly!")
--- [9] MD ---
> **Poznámka:** Funkci `add` jsme zde definovali pøímo v notebooku pro rychlou ukázku.
> V další sekci ji pøesuneme do samostatného souboru — tak se to dìlá v reálných projektech.
--- [10] MD ---
---

## 3. Pytest - testovací framework

Klíèové slovo `assert` je užiteèné, ale pro reálné projekty potøebujeme víc:

- **Automatické spouštìní** všech testù najednou
- **Pøehledný výstup** - co prošlo, co selhalo, a proè
- **Organizaci testù** do souborù a funkcí

K tomu slouží **pytest** - nejpopulárnìjší testovací framework pro Python.

### Instalace

Pytest je externí knihovna (vzpomeòte si na hodinu o knihovnách a pip):
--- [11] CODE (2 ln) ---
# Instalace pytestu (staèí spustit jednou)
!pip install pytest -q
--- [12] MD ---
### Jak pytest funguje?

Pytest pracuje se soubory a funkcemi. Pravidla jsou jednoduchá:

| Pravidlo | Pøíklad |
|---|---|
| Testovací soubor zaèíná na `test_` | `test_calculator.py` |
| Testovací funkce zaèíná na `test_` | `def test_add():` |
| Uvnitø funkce používáme `assert` | `assert add(2, 3) == 5` |

Pytest automaticky najde všechny takové soubory a funkce a spustí je.

### Vytvoøíme si první testovaný modul

Pomocí `%%writefile` zapíšeme Python soubor pøímo z notebooku.
V praxi byste tyto soubory psali ve svém editoru / IDE.
--- [13] CODE (24 ln) ---
%%writefile calculator.py
"""Simple calculator module for testing demonstration."""


def add(a: float, b: float) -> float:
    """Return the sum of two numbers."""
    return a + b


def subtract(a: float, b: float) -> float:
    """Return the difference of two numbers."""
    return a - b


def multiply(a: float, b: float) -> float:
    """Return the product of two numbers."""
    return a * b


def divide(a: float, b: float) -> float:
    """Return the division of a by b. Raises ValueError if b is zero."""
    if b == 0:
        raise ValueError("Cannot divide by zero!")
    return a / b
--- [14] MD ---
Teï napíšeme testovací soubor. Všimnìte si:
- Název souboru zaèíná na `test_`
- Importujeme funkce z našeho modulu `calculator`
- Každá testovací funkce zaèíná na `test_`
- Uvnitø používáme obyèejné `assert`
--- [15] CODE (42 ln) ---
%%writefile test_calculator.py
"""Tests for the calculator module."""
from calculator import add, subtract, multiply, divide


# -- Tests for add --

def test_add_positive_numbers():
    assert add(2, 3) == 5

def test_add_negative_numbers():
    assert add(-1, -1) == -2

def test_add_zero():
    assert add(0, 0) == 0


# -- Tests for subtract --

def test_subtract_basic():
    assert subtract(10, 4) == 6

def test_subtract_negative_result():
    assert subtract(3, 7) == -4


# -- Tests for multiply --

def test_multiply_basic():
    assert multiply(3, 4) == 12

def test_multiply_by_zero():
    assert multiply(5, 0) == 0


# -- Tests for divide --

def test_divide_basic():
    assert divide(10, 2) == 5.0

def test_divide_decimal_result():
    assert divide(7, 2) == 3.5
--- [16] MD ---
### Spuštìní testù

Testy spustíme pøíkazem `pytest` z terminálu. Pøepínaè `-v` (verbose) zobrazí detail každého testu:
--- [17] CODE (1 ln) ---
!pytest test_calculator.py -v
--- [18] MD ---
### Ètení výstupu pytestu

Ve výstupu vidíte:
- **PASSED** (zelená teèka) - test prošel
- Název souboru a funkce u každého testu
- Souhrn na konci: kolik testù prošlo, kolik selhalo

Zkusme teï pøidat test, který **selže**, abychom vidìli, jak pytest zobrazuje chyby:
--- [19] CODE (12 ln) ---
%%writefile test_calculator_fail.py
"""Test file with an intentionally failing test."""
from calculator import add


def test_add_correct():
    assert add(2, 3) == 5


def test_add_wrong_expectation():
    # Tento test SELŽE - úmyslnì špatná oèekávaná hodnota
    assert add(2, 2) == 5
--- [20] CODE (1 ln) ---
!pytest test_calculator_fail.py -v
--- [21] MD ---
Všimnìte si, jak pytest u selhávajícího testu zobrazí:
- **Øádek, kde assert selhal**
- **Skuteènou hodnotu** vs. **oèekávanou hodnotu** (napø. `4 == 5`)
- Pøehledný souhrn: `1 passed, 1 failed`

Toto je jedna z hlavních výhod pytestu oproti holému `assert` - **nemusíte psát chybové zprávy ruènì**, pytest je vygeneruje za vás.
--- [22] MD ---
---

## 4. Vzor Arrange-Act-Assert (AAA)

Pøi psaní testù se osvìdèil jednoduchý vzor se tøemi kroky:

1. **Arrange** (Pøiprav) - Pøipravíme si vstupní data a oèekávané výsledky
2. **Act** (Proveï) - Zavoláme testovanou funkci
3. **Assert** (Ovìø) - Zkontrolujeme, že výsledek odpovídá oèekávání

Tento vzor pomáhá udržet testy **pøehledné a èitelné**. Podívejme se na pøíklad
s naší kalkulaèkou:
--- [23] CODE (41 ln) ---
%%writefile test_calculator_aaa.py
"""Tests demonstrating the Arrange-Act-Assert pattern."""
from calculator import add, subtract, multiply, divide


def test_add_exam_scores():
    # ARRANGE - pøipravíme si data
    score1 = 85
    score2 = 92
    expected_total = 177

    # ACT - zavoláme testovanou funkci
    result = add(score1, score2)

    # ASSERT - ovìøíme výsledek
    assert result == expected_total


def test_calculate_change():
    # ARRANGE
    price = 350
    paid = 500
    expected_change = 150

    # ACT
    change = subtract(paid, price)

    # ASSERT
    assert change == expected_change


def test_total_price_with_quantity():
    # ARRANGE
    unit_price = 49.90
    quantity = 3

    # ACT
    total = multiply(unit_price, quantity)

    # ASSERT
    assert total == 149.70
--- [24] CODE (1 ln) ---
!pytest test_calculator_aaa.py -v
--- [25] MD ---
Všimnìte si, jak komentáøe `# ARRANGE`, `# ACT`, `# ASSERT` pomáhají vizuálnì
oddìlit tøi fáze testu. Nemusíte je psát pokaždé, ale zpoèátku vám pomohou
udržet testy pøehledné.

> **Tip:** Pokud je váš test delší než ~10 øádkù, AAA komentáøe výraznì
> zlepší èitelnost i pro ostatní (nebo pro vás za mìsíc).
--- [26] MD ---
---

## 5. Hranièní pøípady (Edge Cases)

Toto je pravdìpodobnì **nejdùležitìjší dovednost** spojená s testováním.
Napsat test na "normální" pøípad je snadné. Skuteèná hodnota testování je v tom,
že nás nutí pøemýšlet o **situacích, na které bychom jinak zapomnìli**.

### Jak pøemýšlet o edge casech?

Pøi testování jakékoliv funkce se ptejte:

| Otázka | Pøíklad |
|---|---|
| Co když je vstup **prázdný**? | Prázdný seznam, prázdný øetìzec |
| Co když je vstup **nulový**? | `0`, `0.0` |
| Co když je vstup **záporný**? | `-1`, `-100` |
| Co když je vstup **velmi velký**? | `10**18`, milion prvkù |
| Co když je vstup **neoèekávaného typu**? | String místo èísla |
| Co když je vstup na **hranici** podmínky? | Pøesnì `0`, pøesnì `100` |

### Ukázka: testování funkce pro výpoèet prùmìru

Napišme funkci `average` a pokusme se ji dùkladnì otestovat:
--- [27] CODE (12 ln) ---
%%writefile stats.py
"""Simple statistics module."""


def average(numbers: list[float]) -> float:
    """Return the arithmetic mean of a list of numbers.
    
    Raises ValueError if the list is empty.
    """
    if not numbers:
        raise ValueError("Cannot compute average of empty list")
    return sum(numbers) / len(numbers)
--- [28] CODE (57 ln) ---
%%writefile test_stats.py
"""Thorough tests for the stats module - demonstrating edge case thinking."""
import pytest
from stats import average


# --- Normální (happy path) pøípady ---

def test_average_basic():
    """Prùmìr z nìkolika kladných èísel."""
    assert average([1, 2, 3, 4, 5]) == 3.0


def test_average_two_numbers():
    """Prùmìr ze dvou èísel."""
    assert average([10, 20]) == 15.0


# --- Hranièní pøípady ---

def test_average_single_element():
    """Prùmìr ze seznamu s jedním prvkem = ten prvek."""
    assert average([42]) == 42.0


def test_average_negative_numbers():
    """Prùmìr ze záporných èísel."""
    assert average([-1, -2, -3]) == -2.0


def test_average_mixed_positive_negative():
    """Prùmìr ze smìsi kladných a záporných èísel."""
    assert average([-10, 10]) == 0.0


def test_average_with_zeros():
    """Prùmìr ze samých nul."""
    assert average([0, 0, 0]) == 0.0


def test_average_decimal_numbers():
    """Prùmìr z desetinných èísel."""
    result = average([1.5, 2.5])
    assert result == 2.0


def test_average_large_numbers():
    """Prùmìr z velkých èísel - ovìøíme, že nedojde k pøeteèení."""
    assert average([10**9, 10**9]) == 10**9


# 
--- [29] CODE (1 ln) ---
!pytest test_stats.py -v
--- [30] MD ---
Všimnìte si, jak jsou testy **organizované** do skupin:
- **Happy path** - normální, oèekávané použití
- **Hranièní pøípady** - jeden prvek, nuly, záporná èísla, velká èísla
- **Chybové pøípady** - co se stane pøi neplatném vstupu

Toto èlenìní je dobrý zvyk - pomáhá ujistit se, že na nic nezapomenete.
--- [31] MD ---
### Pozor na desetinná èísla (floating point)

Pøi testování výpoètù s desetinnými èísly narazíte na záludný problém.
Podívejte se na následující pøíklad:
--- [32] CODE (3 ln) ---
# Tohle by mìlo být True, ne?
print(f"0.1 + 0.2 = {0.1 + 0.2}")
print(f"0.1 + 0.2 == 0.3? {0.1 + 0.2 == 0.3}")  # False!
--- [33] MD ---
**Proè?** Poèítaèe ukládají desetinná èísla v **binárním formátu** (IEEE 754 floating point).
Nìkterá èísla, která vypadají jednoduše v desítkové soustavì (jako `0.1`), 
**nelze pøesnì reprezentovat** v binární soustavì - podobnì jako 1/3 = 0.333... 
nelze pøesnì zapsat v desítkové soustavì.

Výsledek `0.1 + 0.2` proto není pøesnì `0.3`, ale `0.30000000000000004`.

### Jak to øešit v testech?

**Špatnì** - pøímé porovnání selže:
--- [34] CODE (2 ln) ---
# ŠPATNÌ - tento assert selže!
assert 0.1 + 0.2 == 0.3, f"Dostali jsme {0.1 + 0.2}, ne 0.3"
--- [35] MD ---
**Správnì** - použijeme `pytest.approx`, který porovnává s malou tolerancí:
--- [36] CODE (10 ln) ---
